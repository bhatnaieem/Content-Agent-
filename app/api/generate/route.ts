import { NextResponse } from "next/server";
import { generateWithLLM } from "../../../lib/llm";
export const maxDuration=60;
type SourceDetail={name:string;url:string;published_at:string};
type ClientProfile={name?:string;description?:string;sector?:string;chains?:string;topics?:string;competitors?:string;audience?:string;tone?:string;objectives?:string;avoid?:string};
type Story={headline:string;category:string;score:number;format:"thread"|"single post";reason:string;summary:string;keywords:string[];hashtags:string[];sources:string[];source_details?:SourceDetail[];posting_time_utc:string;cta:string;graphic_prompt:string;alt_text:string;thread:{title:string;tweets:string[]};engagement?:{reply:string;quote_tweet:string;poll:string;blog_expansion:string}};
const RESEARCH_PROMPT=`You are CryptoPulse, a Web3 PR research and content intelligence agent. Find the TWO strongest recent crypto/Web3 opportunities and return strict JSON. Prioritize genuinely recent, high-impact stories from the latest 48 hours when possible. For every story, provide source details ONLY when you actually know them. NEVER invent a URL, source name, or publication date. Prefer primary/official sources, major crypto publications, regulatory filings, company announcements, and reputable market data. Create the core X thread/post plus metadata and a visual concept.
When a CLIENT PROFILE is supplied, personalize the research: prioritize stories relevant to its sector, chains, topics, audience, objectives and competitors; identify direct PR opportunities for that client; deprioritize unrelated stories; respect its avoid/guardrails. Do not claim a client is involved in a story unless the evidence supports it.
JSON schema:{"date":"YYYY-MM-DD","generated_at_utc":"ISO","stories":[{"headline":"","category":"","score":0,"format":"thread|single post","reason":"","summary":"","keywords":[],"hashtags":[],"sources":[],"source_details":[{"name":"","url":"","published_at":"ISO or YYYY-MM-DD or empty"}],"posting_time_utc":"","cta":"","graphic_prompt":"","alt_text":"","thread":{"title":"","tweets":[]}}]}`;
const FORMAT_PROMPT=`You are the CryptoPulse Content Studio. Create SIX genuinely different assets from the supplied story. Do NOT rewrite the same post six times. REPLY: 1-2 conversational sentences adding one specific insight or question. QUOTE TWEET: 1-3 punchy sentences with a distinct opinion or counterpoint. POLL: concise question plus exactly 4 distinct choices. BLOG: substantial article draft/outline with title, introduction, at least 3 sections, evidence/context, implications and conclusion. CREATIVE: detailed image-generation prompt describing visual scene, composition, subject, lighting, mood, camera/style and Web3 symbolism. ALT_TEXT: concise accessibility description. Return ONLY JSON:{"reply":"","quote_tweet":"","poll":"","blog_expansion":"","creative":"","alt_text":""}`;
function cleanSourceDetails(v:unknown):SourceDetail[]{if(!Array.isArray(v))return[];return v.filter(x=>x&&typeof x==="object").map((x:any)=>({name:typeof x.name==="string"?x.name.trim():"",url:typeof x.url==="string"&&/^https?:\/\//i.test(x.url.trim())?x.url.trim():"",published_at:typeof x.published_at==="string"?x.published_at.trim():""})).filter(x=>x.name||x.url||x.published_at)}
function normalizeStory(s:any):Story{const sd=cleanSourceDetails(s.source_details);const legacy=Array.isArray(s.sources)?s.sources.filter((x:unknown)=>typeof x==="string"):[];return {...s,sources:legacy.length?legacy:sd.map(x=>x.url||x.name).filter(Boolean),source_details:sd}}
function clientContext(c?:ClientProfile){if(!c)return "";return `\n\nCLIENT PROFILE\nName: ${c.name||""}\nDescription: ${c.description||""}\nSector: ${c.sector||""}\nChains/ecosystems: ${c.chains||""}\nPriority topics: ${c.topics||""}\nCompetitors: ${c.competitors||""}\nAudience: ${c.audience||""}\nTone: ${c.tone||""}\nPR objectives: ${c.objectives||""}\nAvoid/guardrails: ${c.avoid||""}`}
export async function POST(request:Request){try{let body:any={};try{body=await request.json()}catch{};let provider:"auto"|"gemini"|"nemotron"="auto";if(["auto","gemini","nemotron"].includes(body?.provider))provider=body.provider;const research=await generateWithLLM({provider,system:RESEARCH_PROMPT,user:`Research the latest high-value Web3/crypto developments and select the two strongest opportunities. Return strict JSON.${clientContext(body?.clientProfile)}`,responseFormat:"json_object",temperature:0.2});const data=JSON.parse(research.content);const stories:Story[]=Array.isArray(data.stories)?data.stories.map(normalizeStory):[];const enriched=await Promise.all(stories.map(async story=>{try{const formats=await generateWithLLM({provider,system:FORMAT_PROMPT,user:`Create the six distinct Content Studio assets for this story.\nHeadline: ${story.headline}\nCategory: ${story.category}\nSummary: ${story.summary}\nReason: ${story.reason}\nSources: ${(story.sources||[]).join(" | ")}\nKeywords: ${(story.keywords||[]).join(", ")}\nCore thread: ${(story.thread?.tweets||[]).join("\n")}${clientContext(body?.clientProfile)}`,responseFormat:"json_object",temperature:0.65});const a=JSON.parse(formats.content);return {...story,graphic_prompt:a.creative||story.graphic_prompt,alt_text:a.alt_text||story.alt_text,engagement:{reply:a.reply||"",quote_tweet:a.quote_tweet||"",poll:a.poll||"",blog_expansion:a.blog_expansion||""}}}catch(error){console.error("Content format generation failed:",error);return {...story,engagement:{reply:"Add a specific reaction or question to this story.",quote_tweet:`This development deserves a closer look: ${story.headline}`,poll:`Question: What matters most here?\nOptions: A) Adoption B) Innovation C) Regulation D) Market impact`,blog_expansion:`${story.headline}\n\nIntroduction\n\n${story.summary}\n\nKey implications\n\n${story.reason}\n\nConclusion\n\n${story.cta}`}}}));return NextResponse.json({...data,stories:enriched,llm_provider:research.provider,llm_model:research.model,client_mode:Boolean(body?.clientProfile),client_name:body?.clientProfile?.name||null})}catch(error:any){console.error("CryptoPulse Generation Error:",error);return NextResponse.json({error:error.message||"Failed to generate content intelligence."},{status:500})}}
