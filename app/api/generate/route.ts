import { NextResponse } from "next/server";
import { generateWithLLM } from "../../../lib/llm";
import { runMultiAgentResearch } from "../../../lib/multi-agent";
import { filterPreviouslyCovered, getUsedCandidateIds, rememberCandidates, saveGeneratedStories, listContentHistory } from "../../../lib/content-memory";

export const runtime = "nodejs";
export const maxDuration = 60;

type SourceDetail = { name: string; url: string; published_at: string };
type ClientProfile = { name?: string; description?: string; sector?: string; chains?: string; topics?: string; competitors?: string; audience?: string; tone?: string; objectives?: string; avoid?: string };
type Story = { headline:string; category:string; score:number; format:"thread"|"single post"; reason:string; summary:string; keywords:string[]; hashtags:string[]; sources:string[]; source_details?:SourceDetail[]; posting_time_utc:string; cta:string; graphic_prompt:string; alt_text:string; thread:{title:string;tweets:string[]}; engagement:{reply:string;quote_tweet:string;poll:string;blog_expansion:string}; candidate_ids:string[] };
type FreshCandidate = { id:string; title:string; url:string; source:string; publishedAt:string; summary:string; category:string; keywords:string[]; score:number; opportunity:string };
type GenerateBody = { provider?:string; clientProfile?:ClientProfile; excludeCandidateIds?:string[] };

const CONTENT_PROMPT = `You are the Web3 Pulse Content Director Agent. Work ONLY from the supplied VERIFIED CURRENT candidates. Do not use model memory to add current events, old facts, invented claims, sources, URLs or dates. PRIORITY ORDER: 1. Exploits & Hacks 2. Airdrops 3. Airdrop Guides 4. Other high-interest Web3. Select FIVE DISTINCT stories whenever at least five eligible candidates are supplied. You MUST return exactly one story for each of the first five distinct eligible candidates supplied, unless a candidate is clearly unusable or duplicates another supplied candidate; if you cannot produce five, produce as many distinct stories as there are eligible candidates. Never collapse multiple candidates into one story merely because they share a category. Do not repeat the same real-world event, protocol, headline or angle across stories. Candidates excluded by the research system must never be recreated. For every story, candidate_ids SHOULD contain the exact supplied candidate id when possible. Preserve exact supplied source URLs. Every story must be traceable to at least one supplied candidate. Generate master thread, single X post, reply, quote tweet, poll with exactly 4 choices, blog expansion, creative/image prompt and alt text. Make every format genuinely different. Never invent airdrop eligibility, tasks, claim links, rewards or deadlines. Never invent security-incident users, losses, attacker identity or remediation. Return ONLY valid JSON with a stories array.`;
function clientContext(client?:ClientProfile){if(!client)return "";return ["","CLIENT PROFILE",`Name: ${client.name||""}`,`Description: ${client.description||""}`,`Sector: ${client.sector||""}`,`Chains/ecosystems: ${client.chains||""}`,`Priority topics: ${client.topics||""}`,`Competitors: ${client.competitors||""}`,`Audience: ${client.audience||""}`,`Tone: ${client.tone||""}`,`PR objectives: ${client.objectives||""}`,`Avoid/guardrails: ${client.avoid||""}`].join("\n");}
function normalizeUrl(url:string){return String(url||"").trim().replace(/\/$/,"");}
function parseExcludedCookie(request:Request):string[]{const raw=request.headers.get("cookie")||"";const match=raw.match(/(?:^|;\s*)web3pulse_excluded=([^;]+)/);if(!match)return [];try{const parsed=JSON.parse(decodeURIComponent(match[1]));return Array.isArray(parsed)?parsed.filter(id=>typeof id==="string").slice(-500):[];}catch{return [];}}
function resolveCandidateIds(story:any,candidates:FreshCandidate[],fallbackIndex?:number):string[]{const byId=new Map(candidates.map(c=>[c.id,c]));const explicit=Array.isArray(story?.candidate_ids)?story.candidate_ids:[story?.candidate_id];const ids=explicit.filter((id:unknown)=>typeof id==="string"&&byId.has(id));if(ids.length)return Array.from(new Set(ids));const urls=new Set<string>();if(Array.isArray(story?.sources))for(const url of story.sources)if(typeof url==="string")urls.add(normalizeUrl(url));if(Array.isArray(story?.source_details))for(const detail of story.source_details)if(typeof detail?.url==="string")urls.add(normalizeUrl(detail.url));const urlMatches=candidates.filter(c=>urls.has(normalizeUrl(c.url))).map(c=>c.id);if(urlMatches.length)return Array.from(new Set(urlMatches));const headline=String(story?.headline||story?.thread?.title||"").toLowerCase().trim();if(headline){const matches=candidates.filter(c=>{const title=c.title.toLowerCase().trim();return title===headline||title.includes(headline)||headline.includes(title);}).map(c=>c.id);if(matches.length)return Array.from(new Set(matches));}if(typeof fallbackIndex==="number"&&candidates[fallbackIndex])return [candidates[fallbackIndex].id];return [];}
function normalizeStory(story:any,candidates:FreshCandidate[],fallbackIndex?:number):Story|null{const ids=resolveCandidateIds(story,candidates,fallbackIndex);if(!ids.length)return null;const candidateMap=new Map(candidates.map(c=>[c.id,c]));const supported=ids.map(id=>candidateMap.get(id)).filter(item=>item!==undefined) as FreshCandidate[];if(!supported.length)return null;const first=supported[0];const threadSource=story?.thread&&typeof story.thread==="object"?story.thread:{};const engagement=story?.engagement&&typeof story.engagement==="object"?story.engagement:{};const tweets=Array.isArray(threadSource.tweets)?threadSource.tweets.filter((x:unknown)=>typeof x==="string"&&x.trim().length>0) as string[]:[];const sourceDetails:SourceDetail[]=supported.map(item=>({name:item.source,url:item.url,published_at:item.publishedAt}));return {headline:String(story?.headline||first.title),category:String(story?.category||first.category||"Emerging"),score:Number.isFinite(Number(story?.score))?Number(story.score):first.score,format:story?.format==="single post"?"single post":"thread",reason:String(story?.reason||first.opportunity||"Current verified Web3 development."),summary:String(story?.summary||first.summary||""),keywords:Array.isArray(story?.keywords)?story.keywords.filter((x:unknown)=>typeof x==="string") as string[]:first.keywords||[],hashtags:Array.isArray(story?.hashtags)?story.hashtags.filter((x:unknown)=>typeof x==="string") as string[]:[],sources:sourceDetails.map(x=>x.url),source_details:sourceDetails,posting_time_utc:String(story?.posting_time_utc||first.publishedAt),cta:String(story?.cta||"Review the source before publishing."),graphic_prompt:String(story?.graphic_prompt||`Create a premium editorial Web3 visual for: ${story?.headline||first.title}.`),alt_text:String(story?.alt_text||`Editorial visual representing ${story?.headline||first.title}.`),thread:{title:String(threadSource.title||story?.headline||first.title),tweets:tweets.length?tweets:[String(story?.summary||first.summary||story?.headline||first.title)]},engagement:{reply:String(engagement.reply||""),quote_tweet:String(engagement.quote_tweet||""),poll:String(engagement.poll||""),blog_expansion:String(engagement.blog_expansion||"")},candidate_ids:ids};}
function parseLLMJson(content:string):any{const cleaned=content.replace(/^\s*```(?:json)?\s*/i,"").replace(/\s*```\s*$/i,"").trim();try{return JSON.parse(cleaned);}catch{}const start=cleaned.indexOf("{");if(start<0)throw new Error("LLM returned no JSON object.");let depth=0,inString=false,escaped=false;for(let i=start;i<cleaned.length;i++){const ch=cleaned[i];if(inString){if(escaped)escaped=false;else if(ch==="\\")escaped=true;else if(ch==='"')inString=false;continue;}if(ch==='"'){inString=true;continue;}if(ch==="{")depth++;else if(ch==="}"&&--depth===0){try{return JSON.parse(cleaned.slice(start,i+1));}catch{break;}}}throw new Error("LLM returned invalid JSON.");}

export async function POST(request:Request){
  const started=Date.now();
  try{
    let body:GenerateBody={};try{body=await request.json();}catch{}
    const provider=["auto","gemini","nemotron","openrouter"].includes(body.provider||"")?(body.provider as "auto"|"gemini"|"nemotron"|"openrouter"):"auto";
    const cookieExcluded=parseExcludedCookie(request);const requestedExcluded=Array.isArray(body.excludeCandidateIds)?body.excludeCandidateIds.filter(id=>typeof id==="string"&&id.trim()):[];
    let databaseUsedIds:string[]=[];try{databaseUsedIds=await getUsedCandidateIds();}catch(error){console.warn("Supabase memory unavailable; using cookie/request exclusions only.",error);}
    const excludedIds=Array.from(new Set([...cookieExcluded,...requestedExcluded,...databaseUsedIds])).slice(-500);const now=new Date();const currentDate=now.toISOString().slice(0,10);const cutoffUtc=new Date(now.getTime()-72*60*60*1000).toISOString();
    const packet=await runMultiAgentResearch(excludedIds);
    let freshCandidates:FreshCandidate[]=packet.candidates.filter(item=>{const t=Date.parse(item.publishedAt);return Number.isFinite(t)&&t>=Date.parse(cutoffUtc)&&t<=now.getTime();}).map(item=>({id:item.id,title:item.title,url:item.url,source:item.source,publishedAt:item.publishedAt,summary:item.summary,category:item.category,keywords:item.keywords,score:item.scores.overall,opportunity:item.opportunity}));
    try{await rememberCandidates(freshCandidates);freshCandidates=await filterPreviouslyCovered(freshCandidates);}catch(error){console.warn("Supabase duplicate memory unavailable during candidate filtering.",error);}
    if(!freshCandidates.length){let fallbackStories:Story[]=[];try{const history=await listContentHistory(5);fallbackStories=history.map((row:any)=>{const content=row.content;const story=content&&typeof content==="object"?content:null;return story&&typeof story.headline==="string"?story as Story:null;}).filter((s:Story|null):s is Story=>Boolean(s));}catch(error){console.warn("History fallback unavailable.",error);}const researchFailed=packet.raw_candidate_count===0;if(fallbackStories.length)return NextResponse.json({date:currentDate,generated_at_utc:now.toISOString(),stories:fallbackStories,briefing_fallback:true,no_new_stories:!researchFailed,research_failed:researchFailed,message:"No new eligible stories were found, so the latest saved verified briefing is shown while the research window refreshes.",code:"HISTORY_FALLBACK",cutoff_utc:cutoffUtc,excluded_count:excludedIds.length,agents:packet.agents,verification:packet.verification,research_sources:packet.narratives,source_health:packet.source_health,raw_candidate_count:packet.raw_candidate_count,fresh_candidate_count:packet.fresh_candidate_count,llm_calls:0,elapsed_ms:Date.now()-started,memory:"supabase"},{status:200});return NextResponse.json({date:currentDate,generated_at_utc:now.toISOString(),stories:[],no_new_stories:!researchFailed,research_failed:researchFailed,error:researchFailed?"Research sources returned no fresh candidates.":"No new verified stories are available in the current 72-hour research window.",code:researchFailed?"RESEARCH_EMPTY":"NO_NEW_STORIES",cutoff_utc:cutoffUtc,excluded_count:excludedIds.length,agents:packet.agents,verification:packet.verification,research_sources:packet.narratives,source_health:packet.source_health,raw_candidate_count:packet.raw_candidate_count,fresh_candidate_count:packet.fresh_candidate_count,llm_calls:0,elapsed_ms:Date.now()-started,memory:"supabase"},{status:200});}
    const context=[`CURRENT_DATE: ${currentDate}`,`CURRENT_TIME_UTC: ${now.toISOString()}`,`CUTOFF_UTC: ${cutoffUtc}`,"Only the supplied candidates may be used as factual sources.",`MULTI_AGENT_REPORTS: ${JSON.stringify(packet.agents)}`,`VERIFICATION: ${JSON.stringify(packet.verification)}`,`PRIORITY_FOCUS: ${packet.priority_focus.join(", ")}`,`NARRATIVES: ${JSON.stringify(packet.narratives.slice(0,8))}`].join("\n");
    const targetCount=Math.min(5,freshCandidates.length);const candidateContext=`VERIFIED CURRENT CANDIDATES (${freshCandidates.length}):\n${JSON.stringify(freshCandidates,null,2)}\n\nMANDATORY OUTPUT COUNT: ${targetCount}. Return ${targetCount} distinct stories. One story must map to each of the first ${targetCount} distinct candidates by candidate_id. Do not merge them.`;const userPrompt=`${context}\n\n${candidateContext}\n\nGenerate the COMPLETE CONTENT PACKAGE.`+clientContext(body.clientProfile);
    let generation=await generateWithLLM({provider,system:CONTENT_PROMPT,user:userPrompt,responseFormat:"json_object",temperature:0.2});let data:any;try{data=parseLLMJson(generation.content);}catch{return NextResponse.json({error:"The selected LLM returned malformed structured output.",code:"INVALID_LLM_JSON",provider:generation.provider,model:generation.model,date:currentDate},{status:502});}
    let rawStories=Array.isArray(data.stories)?data.stories:[];let stories=rawStories.map((s:any,index:number)=>normalizeStory(s,freshCandidates,index)).filter((s:Story|null):s is Story=>s!==null);let freshStories:Story[]=stories.filter((s:Story)=>s.candidate_ids.every((id:string)=>{const candidate=freshCandidates.find(item=>item.id===id);const t=candidate?Date.parse(candidate.publishedAt):NaN;return Number.isFinite(t)&&t>=Date.parse(cutoffUtc)&&t<=now.getTime();}));
    if(freshStories.length<targetCount&&targetCount>1){const retryPrompt=`${context}\n\n${candidateContext}\n\nIMPORTANT RETRY: Your previous response returned fewer than the required ${targetCount} stories. Return EXACTLY ${targetCount} stories now. Use one distinct candidate_id per story from these eligible candidates: ${freshCandidates.slice(0,targetCount).map(c=>c.id).join(", ")}. Do not merge candidates. Keep every story fully grounded in its assigned candidate. Return ONLY JSON with stories array.`+clientContext(body.clientProfile);try{generation=await generateWithLLM({provider,system:CONTENT_PROMPT,user:retryPrompt,responseFormat:"json_object",temperature:0.25});data=parseLLMJson(generation.content);rawStories=Array.isArray(data.stories)?data.stories:[];stories=rawStories.map((s:any,index:number)=>normalizeStory(s,freshCandidates,index)).filter((s:Story|null):s is Story=>s!==null);freshStories=stories.filter((s:Story)=>s.candidate_ids.every((id:string)=>{const candidate=freshCandidates.find(item=>item.id===id);const t=candidate?Date.parse(candidate.publishedAt):NaN;return Number.isFinite(t)&&t>=Date.parse(cutoffUtc)&&t<=now.getTime();}));}catch(error){console.warn("Multi-story retry failed",error);}}
    if(!freshStories.length)return NextResponse.json({error:"The LLM returned no story grounded in the verified current candidates.",code:"NO_GROUNDED_STORIES",date:currentDate,candidate_count:freshCandidates.length,raw_story_count:rawStories.length,llm_provider:generation.provider,llm_model:generation.model},{status:503});
    try{await saveGeneratedStories(freshStories.map(story=>({headline:story.headline,category:story.category,candidate_ids:story.candidate_ids,source_urls:story.sources,content:story,status:"generated",generated_at:now.toISOString(),llm_provider:generation.provider,llm_model:generation.model})))}catch(error){console.error("Supabase content memory write failed:",error);return NextResponse.json({error:"Content was generated but Web3 Pulse could not save it to persistent memory. Nothing was returned to prevent duplicate generation.",code:"MEMORY_WRITE_FAILED",detail:error instanceof Error?error.message:"Supabase write failed"},{status:503});}
    const response=NextResponse.json({...data,date:currentDate,generated_at_utc:now.toISOString(),stories:freshStories,research_window:{cutoff_utc:cutoffUtc,hours:72,candidate_count:freshCandidates.length,excluded_candidate_count:excludedIds.length},research_sources:packet.narratives,source_health:packet.source_health,raw_candidate_count:packet.raw_candidate_count,fresh_candidate_count:packet.fresh_candidate_count,agents:packet.agents,verification:packet.verification,llm_provider:generation.provider,llm_model:generation.model,client_mode:Boolean(body.clientProfile),client_name:body.clientProfile?.name||null,priority_focus:packet.priority_focus,llm_calls:freshStories.length<targetCount?2:1,elapsed_ms:Date.now()-started,memory:"supabase"});const newlyUsed=freshStories.flatMap((s:Story)=>s.candidate_ids);response.cookies.set("web3pulse_excluded",encodeURIComponent(JSON.stringify(Array.from(new Set([...excludedIds,...newlyUsed])).slice(-500))),{httpOnly:true,sameSite:"lax",secure:true,path:"/",maxAge:60*60*24*30});return response;
  }catch(error:any){console.error("Web3 Pulse Generation Error:",error);return NextResponse.json({error:error?.message||"Failed to generate current Web3 Pulse intelligence.",code:"GENERATION_ERROR",elapsed_ms:Date.now()-started},{status:500});}
}
